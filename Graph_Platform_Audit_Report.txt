ТЕХНИЧЕСКИЙ АУДИТ GRAPH PLATFORM 3.2.0
Относительно Graph_Platform_Requirements.md
Дата проверки: 28.07.2026
Репозиторий: /Users/karengevorgyan/Documents/boo/graph-platform
Базовый commit: e1ea64c6ef7cdeded1244c29ecf3da296b986102
Объект проверки: текущее рабочее дерево с изменениями версии 3.2.0

Правило отчёта: ниже отмечено только то, что подтверждено файлами, применённой SQLite-схемой, HTTP-запросами или выводом команд. PostgreSQL исключён из требований по прямому указанию заказчика; согласованная БД — SQLite WAL.

## 1. Технический стек

### 1.1 Пакетный менеджер

Команды:

```text
$ node --version
v24.16.0

$ npm --version
11.13.0
```

Пакетный менеджер: npm 11.13.0.
Lock-файлы: backend/package-lock.json и frontend/package-lock.json.

Чистая установка выполнена командой `sh scripts/audit.sh`:

```text
added 48 packages, and audited 49 packages in 1s
found 0 vulnerabilities

added 120 packages, and audited 121 packages in 14s
found 0 vulnerabilities
```

### 1.2 Backend

Файл: backend/package.json.

```text
$ npm list --depth=0
graph-platform-backend@3.2.0
├── bcryptjs@2.4.3
├── better-sqlite3@11.10.0
├── cors@2.8.6
├── express@4.22.2
└── jsonwebtoken@9.0.3
```

Фреймворк: Express 4.22.2.
Версия приложения: 3.2.0.
Порт по умолчанию: 3001 (`Number(process.env.PORT || 3001)`).
Фактический изолированный порт аудита: 31991.

### 1.3 Frontend

Файл: frontend/package.json.

```text
$ npm list --depth=0
graph-platform-frontend@3.2.0
├── @types/react-dom@18.3.7
├── @types/react@18.3.31
├── @vitejs/plugin-react@6.0.4
├── @xyflow/react@12.11.2
├── react-dom@18.3.1
├── react@18.3.1
├── typescript@5.6.3
├── vite@8.1.5
└── zustand@5.0.14
```

Frontend: React 18.3.1 + TypeScript 5.6.3 + React Flow (`@xyflow/react`) 12.11.2.
Dev-порт Vite по умолчанию: 5173.

### 1.4 База данных

Движок: SQLite.
Драйвер: better-sqlite3 11.10.0.
ORM: отсутствует; используются подготовленные SQL-запросы better-sqlite3.
Режим: WAL.
Foreign keys: включены.
Busy timeout: 5000 ms.
Файл по умолчанию: backend/data/graph.db.
Изолированный аудит: временный файл SQLite, создаваемый scripts/audit.sh.

Фрагмент backend/src/db/database.js:

```js
_db = new Database(DB_PATH);
_db.pragma('journal_mode = WAL');
_db.pragma('foreign_keys = ON');
_db.pragma('busy_timeout = 5000');
```

### 1.5 OpenAPI

✅ OpenAPI-спецификация существует: backend/openapi.yaml.
✅ Спецификация отдаётся по GET /api/openapi.yaml.
✅ YAML синтаксически разобран.
✅ Число документированных операций совпадает с runtime-инвентаризацией.

```text
$ python3 -c "import yaml; ..."
version 3.2.0
paths 108
operations 146

$ wc -l -c backend/openapi.yaml
296 26221 backend/openapi.yaml

$ curl -sS -o /dev/null -w 'HTTP %{http_code}\n' http://127.0.0.1:3110/api/openapi.yaml
HTTP 200
```

### 1.6 Миграция стека

SQLite → PostgreSQL: ❌ не выполнялась, поскольку PostgreSQL исключён заказчиком из текущего объёма.

Версионированные SQLite-миграции: ✅ выполняются автоматически через `schema_migrations`.

```json
[
  {"version":1,"name":"legacy-v3-graph-scoping"},
  {"version":2,"name":"requirements-completion-sqlite"},
  {"version":3,"name":"issue-pipe-execution-links"},
  {"version":4,"name":"execution-object-scoping"},
  {"version":5,"name":"copilot-question-semantics"},
  {"version":6,"name":"customer-owner-separation"},
  {"version":7,"name":"edge-endpoint-foreign-keys"},
  {"version":8,"name":"complete-review-scope-hierarchy"},
  {"version":9,"name":"coordinated-transformation-and-workspace-sharing"},
  {"version":10,"name":"hierarchical-rbac-and-object-acl"}
]
```

Проверка применённой БД:

```json
{"edgeForeignKeys":["target","source","graph_id","workspace_id"],"foreignKeyCheck":[]}
```

## 2. База данных / Схема

### 2.1 Фактически применённая схема

Команда выполнялась на изолированной БД после старта backend:

```text
TABLES=58
actors
answers
artifact_versions
artifacts
change_artifacts
changes
chunks
conversations
decisions
documents
edge_types
edges
epics
features
fragments
graphs
issue_pipes
issues
memberships
node_types
nodes
ontology
object_acl
pipe_work_items
pipes
portfolios
programs
projects
project_node_links
project_resource_links
questions
ratings
reasoning_steps
releases
review_history
review_scopes
review_votes
reviews
role_bindings
rbac_assignments
rbac_permissions
rbac_role_permissions
rbac_roles
schema_migrations
security_audit_log
self_host_sources
sprint_pipes
sprint_work_items
sprints
templates
transformation_alignments
transformation_graph_nodes
transformation_graphs
transformation_sets
users
work_items
workspaces
workspace_resources
```

`PRAGMA foreign_key_check`:

```text
[]
```

### 2.2 Node

✅ Таблица `nodes` существует.

```sql
CREATE TABLE IF NOT EXISTS nodes (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
```

### 2.3 Edge

✅ Таблица `edges` существует.
✅ `source` и `target` имеют внешние ключи на `nodes(id)` с `ON DELETE CASCADE`.

```sql
CREATE TABLE IF NOT EXISTS edges (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
  edge_type_id TEXT,
```

### 2.4 NodeType

✅ Таблица `node_types` существует.

```sql
CREATE TABLE IF NOT EXISTS node_types (
  id TEXT NOT NULL,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  graph_id TEXT NOT NULL REFERENCES graphs(id) ON DELETE CASCADE,
  label TEXT NOT NULL,
```

### 2.5 EdgeType

✅ Таблица `edge_types` существует.

```sql
CREATE TABLE IF NOT EXISTS edge_types (
  id TEXT NOT NULL,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  graph_id TEXT NOT NULL REFERENCES graphs(id) ON DELETE CASCADE,
  label TEXT NOT NULL,
```

### 2.6 Actor

✅ Таблица `actors` существует.
✅ Тип ограничен значениями Human, AIAgent, Service, ExternalSystem.

```sql
CREATE TABLE IF NOT EXISTS actors (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
  type TEXT NOT NULL CHECK(type IN ('Human','AIAgent','Service','ExternalSystem')),
```

### 2.7 Workspace

✅ Таблица `workspaces` существует.
✅ Иерархия workspace поддерживается полем `parent_id` с FK.

```sql
CREATE TABLE IF NOT EXISTS workspaces (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  type TEXT NOT NULL DEFAULT 'company',
  parent_id TEXT REFERENCES workspaces(id) ON DELETE SET NULL,
```

### 2.8 Portfolio

✅ Таблица `portfolios` существует.

```sql
CREATE TABLE IF NOT EXISTS portfolios (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
```

### 2.9 Project

✅ Таблица `projects` существует.
✅ Проект связан с Portfolio и Program.

```sql
CREATE TABLE IF NOT EXISTS projects (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  portfolio_id TEXT REFERENCES portfolios(id) ON DELETE SET NULL,
  program_id TEXT REFERENCES programs(id) ON DELETE SET NULL,
```

### 2.10 Graph

✅ Таблица `graphs` существует.
✅ Каждый новый граф автоматически получает `project_id`.

```sql
CREATE TABLE IF NOT EXISTS graphs (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT REFERENCES projects(id) ON DELETE SET NULL,
  name TEXT NOT NULL,
```

### 2.11 Issue

✅ Таблица `issues` существует.
✅ Типы: Problem, Risk, Constraint, KnowledgeDefect.

```sql
CREATE TABLE IF NOT EXISTS issues (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT REFERENCES projects(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
```

### 2.12 WorkItem

✅ Таблица `work_items` существует.
✅ Есть связи с Issue, Change, Pipe, Release.
✅ Есть часы, специалисты, бюджет, срок, критический путь и риск.

```sql
CREATE TABLE IF NOT EXISTS work_items (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
```

Проверенные дополнительные колонки:

```text
issue_id, change_id, pipe_id, release_id,
estimated_hours, required_specialists_json, budget,
deadline, critical_path, risk_level
```

### 2.13 Review

✅ Таблица `reviews` существует.

```sql
CREATE TABLE IF NOT EXISTS reviews (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
  n INTEGER,
```

### 2.14 ReviewScope

✅ Таблица `review_scopes` существует.
✅ Scope хранит Project → Epic → Feature → Artifact → Version → Fragment.

```sql
CREATE TABLE IF NOT EXISTS review_scopes (
  id TEXT PRIMARY KEY,
  review_id TEXT NOT NULL REFERENCES reviews(id) ON DELETE CASCADE,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT REFERENCES projects(id) ON DELETE SET NULL,
  epic_id TEXT REFERENCES epics(id) ON DELETE SET NULL,
  feature_id TEXT REFERENCES features(id) ON DELETE SET NULL,
  artifact_id TEXT REFERENCES artifacts(id) ON DELETE SET NULL,
  version_id TEXT REFERENCES artifact_versions(id) ON DELETE SET NULL,
  fragment_id TEXT REFERENCES fragments(id) ON DELETE SET NULL,
```

Дополнительно существуют `review_history` и `review_votes`.

### 2.15 Sprint

✅ Таблица `sprints` существует.

```sql
CREATE TABLE IF NOT EXISTS sprints (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT REFERENCES projects(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
```

### 2.16 Pipe

✅ Таблица `pipes` существует.
✅ Стадии конфигурируются через `stages_json`.

```sql
CREATE TABLE IF NOT EXISTS pipes (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT REFERENCES projects(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
```

### 2.17 Release

✅ Таблица `releases` существует.

```sql
CREATE TABLE IF NOT EXISTS releases (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  project_id TEXT REFERENCES projects(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
```

### 2.18 Conversation

✅ Таблица `conversations` существует.

```sql
CREATE TABLE IF NOT EXISTS conversations (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
  graph_id TEXT REFERENCES graphs(id) ON DELETE CASCADE,
  session_id TEXT NOT NULL,
```

### 2.19 Question

✅ Таблица `questions` существует.
✅ Сохраняются intent, actor, role, tab, diagram, стоимость, токены, latency и feedback.

```sql
CREATE TABLE IF NOT EXISTS questions (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL,
  graph_id TEXT REFERENCES graphs(id) ON DELETE SET NULL,
  conversation_id TEXT REFERENCES conversations(id) ON DELETE CASCADE,
```

### 2.20 Answer

✅ Таблица `answers` существует.

```sql
CREATE TABLE IF NOT EXISTS answers (
  id TEXT PRIMARY KEY,
  conversation_id TEXT NOT NULL REFERENCES conversations(id) ON DELETE CASCADE,
  question_id TEXT NOT NULL UNIQUE REFERENCES questions(id) ON DELETE CASCADE,
  workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
```

Дополнительно существуют `reasoning_steps` и `decisions`.

### 2.21 Итог по сущностям ТЗ

```text
Таблиц есть / нужно по ТЗ: 19 / 19
Всего прикладных таблиц в SQLite: 58
Таблиц из списка ТЗ, присутствующих только в коде, но не в БД: 0
Отсутствующих таблиц из списка ТЗ: 0
```

## 3. API

### 3.1 Инвентаризация

Runtime-инвентаризация Express:

```text
ROUTES=146
```

OpenAPI:

```text
paths=108
operations=146
```

Итог: 146 / 146 runtime-операций отражены в OpenAPI.

### 3.2 Health / список движков

```bash
curl -sS -w '\nHTTP %{http_code}\n' http://127.0.0.1:3110/api/health
```

```text
{"ok":true,"version":"3.2.0","db":"sqlite","graphs":10,"llmConfigured":false,"llmMode":"hybrid","offlineAiOptional":true,"offlineAiEnabled":false,"engines":["Graph","FSM","Review","Ontology","RAG","Visualization","Execution","Transformation Coordination","RBAC/ACL","Optional Offline AI","Auth","Workspace","Templates"],"publicDomains":true,"tenantIsolation":"membership + public-read"}
HTTP 200
```

### 3.3 Graph Engine

Рабочие маршруты:

```text
GET    /api/graphs
POST   /api/graphs
PATCH  /api/graphs/:id
DELETE /api/graphs/:id
POST   /api/graphs/import
GET    /api/graph/nodes
POST   /api/graph/nodes
PATCH  /api/graph/nodes/:id
DELETE /api/graph/nodes/:id
GET    /api/graph/edges
POST   /api/graph/edges
PATCH  /api/graph/edges/:id
DELETE /api/graph/edges/:id
GET    /api/graph/neighbors/:id
```

```bash
curl -sS -o /dev/null -w 'HTTP %{http_code}\n' \
  'http://127.0.0.1:3110/api/public/domains/dentist-choice/graph?tab=tobe'
```

```text
HTTP 200
```

Smoke-факт:

```text
[OK] create node with stored layout
[OK] edit node and persist drag layout
[OK] create relation between graph nodes
[OK] cross-graph relation blocked
[OK] edit and read relation
[OK] delete relation
[OK] delete nodes with relation cleanup
```

### 3.4 FSM Engine

Рабочие маршруты:

```text
GET  /api/fsm/machines
GET  /api/fsm/:id/transitions
POST /api/fsm/:id/transition
GET  /api/work-items/:id/transitions
POST /api/work-items/:id/transition
```

```bash
curl -sS -w '\nHTTP %{http_code}\n' \
  -H 'X-Api-Key: dev-api-key' \
  -H 'X-Graph-Id: 592b3031-aaaa-aaaa-aaaa-aaaaaaaaaaaa' \
  'http://127.0.0.1:3110/api/fsm/Task/transitions?status=open'
```

```text
{"type":"Task","allowed":["START","CANCEL"]}
HTTP 200
```

Smoke-факт:

```text
[OK] FSM machine and transition
[OK] Issue to WorkItem hierarchy, metrics and persisted FSM transition
```

### 3.5 Review Engine

Рабочие маршруты:

```text
GET    /api/reviews
POST   /api/reviews
PATCH  /api/reviews/:id
DELETE /api/reviews/:id
POST   /api/reviews/:id/votes
POST   /api/reviews/:id/transition
GET    /api/reviews/:id/history
```

```bash
curl -sS -o /dev/null -w 'HTTP %{http_code}\n' \
  -H 'X-Graph-Id: 592b3031-aaaa-aaaa-aaaa-aaaaaaaaaaaa' \
  http://127.0.0.1:3110/api/reviews
```

```text
HTTP 200
```

Smoke-факт:

```text
[OK] seeded reviews have normalized ReviewScope and status
[OK] ReviewScope, author/executor, voting, approval FSM and immutable history
```

### 3.6 Ontology Engine

Рабочие маршруты:

```text
GET  /api/ontology
POST /api/ontology/extend
GET  /api/node-types
POST /api/node-types
GET  /api/edge-types
POST /api/edge-types
```

```bash
curl -sS -o /dev/null -w 'HTTP %{http_code}\n' \
  -H 'X-Graph-Id: 592b3031-aaaa-aaaa-aaaa-aaaaaaaaaaaa' \
  http://127.0.0.1:3110/api/ontology
```

```text
HTTP 200
```

Smoke-факт:

```text
[OK] default ontology and graph-scoped extension
[OK] NodeType and EdgeType are materialized entities
```

### 3.7 RAG Engine

Рабочие маршруты:

```text
GET  /api/rag/documents
POST /api/rag/ingest
GET  /api/rag/search
```

```bash
curl -sS -w '\nHTTP %{http_code}\n' \
  -H 'X-Api-Key: dev-api-key' \
  -H 'X-Graph-Id: 592b3031-aaaa-aaaa-aaaa-aaaaaaaaaaaa' \
  'http://127.0.0.1:3110/api/rag/search?q=lineage'
```

```text
[{"chunkId":"0da82861-4515-4df8-9ccc-fca7bc0f35d9","documentId":"5c9c0217-b7c7-448f-8caf-f6df16830929","text":"Форма 0409101 — оборотная ведомость. КС — контрольные соотношения. ФЛК — форматно-логический контроль ЦБ. Control Knowledge — контрольные отчёты, КС, DELTA. Экономист отвечает за сдачу. ODS — единый слой. Lineage — путь цифры до операции.","score":1.6,"nodeIds":["rep","ctrl","ods","econ"],"graphId":"592b3031-aaaa-aaaa-aaaa-aaaaaaaaaaaa"}]
HTTP 200
```

### 3.8 Execution / Issue / Change

Рабочие группы маршрутов:

```text
/api/issues
/api/work-items
/api/changes
/api/portfolios
/api/programs
/api/projects
/api/sprints
/api/pipes
/api/releases
/api/sprints/:id/pipes
/api/sprints/:id/work-items
/api/pipes/:id/work-items
/api/issues/:id/pipes
/api/execution-graph
/api/transformation-metrics
```

```bash
curl -sS -o /dev/null -w 'HTTP %{http_code}\n' \
  -H 'X-Api-Key: dev-api-key' \
  -H 'X-Graph-Id: 592b3031-aaaa-aaaa-aaaa-aaaaaaaaaaaa' \
  http://127.0.0.1:3110/api/execution-graph
```

```text
HTTP 200
```

Smoke-факт:

```text
[OK] Issue to WorkItem hierarchy, metrics and persisted FSM transition
[OK] Change links SQL/test perspectives and stores executor metrics
[OK] Portfolio/Program/Project/Pipe/Release graph and independent Sprint-Pipe M:N links
[OK] Transformation Graph aggregates four-layer resource and financial metrics
```

### 3.9 Visualization Engine / PI

Backend curl к Visualization Engine не применим: визуализация реализована на frontend.

Файлы:

```text
frontend/src/components/FlowCanvas.tsx
frontend/src/graphLayout.ts
frontend/src/graphFocus.ts
frontend/src/components/DeliveryControlPanel.tsx
```

Автотест:

```text
✔ keeps an already safe layout unchanged
✔ separates duplicate and too-close coordinates
✔ recovers from invalid imported coordinates
tests 3, pass 3, fail 0
```

### 3.10 Graph Copilot

Рабочие маршруты:

```text
POST  /api/copilot/chat
GET   /api/copilot/history
GET   /api/conversations
POST  /api/conversations
GET   /api/conversations/:id
GET   /api/answers
PATCH /api/answers/:id/feedback
GET   /api/conversations/:id/decisions
POST  /api/conversations/:id/decisions
```

```bash
curl -sS -w '\nHTTP %{http_code}\n' \
  -H 'Content-Type: application/json' \
  -H 'X-Graph-Id: 592b3031-aaaa-aaaa-aaaa-aaaaaaaaaaaa' \
  -H 'X-Session-Id: strict-audit' \
  --data '{"message":"Что такое Lineage?","selectedNodeIds":["core"],"tab":"tobe"}' \
  http://127.0.0.1:3110/api/copilot/chat
```

Сокращённый фактический ответ:

```text
model=graph-copilot-local-v2
offline=true
questionId=464c2a19-b1ff-42e7-aa3b-72a1e03d1468
answerId=8e50cb8a-3793-44fe-985c-5b80ed710446
conversationId=99353dfe-9460-42ec-b956-71e149b18af8
sources.nodes=[core,reg,ods,rep,ctrl,dom,proc,ai,stand,valid,migr,aian,self-graph]
sources.rag=[5c9c0217-b7c7-448f-8caf-f6df16830929]
usage.latencyMs=9
HTTP 200
```

Smoke-факт:

```text
[OK] Copilot persists Conversation, Question, Answer, Reasoning, Decision and usage feedback
```

### 3.11 Self-hosting Engine

Рабочие маршруты:

```text
GET  /api/self-host/status
GET  /api/self-host/sources
POST /api/self-host/sync
```

```bash
curl -sS -w '\nHTTP %{http_code}\n' http://127.0.0.1:3110/api/self-host/status
```

```text
{"running":false,"graphId":"3d5b4a12-4ce8-44f7-b689-d995913ad6ee","root":"/Users/karengevorgyan/Documents/boo/graph-platform","files":71,"changed":71,"lastSyncAt":"2026-07-27T18:34:04.412Z","error":null,"intervalMs":15000,"automatic":false,"sources":71}
HTTP 200
```

В production/dev автоматический интервал включён по умолчанию; в изолированном curl-процессе он отключён переменной `SELF_HOST_WATCH=0`. Отдельный тест изменяет отслеживаемый файл и проверяет изменение hash и узла графа.

### 3.12 Отсутствующие API из списка ТЗ

```text
/api/issues        — существует
/api/work-items     — существует
/api/sprints        — существует
/api/pipes          — существует
/api/releases       — существует
/api/conversations  — существует
/api/answers        — существует
```

Отсутствующих групп API из списка ТЗ: 0.

## 4. Проверка 16 требований ТЗ

### 4.1 Инкрементальная валидация на внутренних экосистемах

✅ Выполнено.

Создаются проекты и графы:

```json
["fsm-engine","greenmarket","map","platform-core","taxi","voice"]
```

Smoke:

```text
[OK] internal validation ecosystems and self-hosting graph
```

### 4.2 Self-hosting графа

✅ Выполнено.

`backend/src/services/selfHosting.js` сканирует исходники и документацию, вычисляет SHA-256, создаёт/обновляет узлы категорий Architecture Decisions, Changes & Verification, Components, Documentation и запускает автоматическую синхронизацию каждые 15000 ms.

Тест:

```text
✔ Self-hosting Engine updates graph nodes when a tracked source changes
```

### 4.3 Project Knowledge Layer / Change

✅ Выполнено.

Таблицы `changes` и `change_artifacts` связывают изменение с узлами в перспективах:

```text
form, indicator, sql, test, document, architecture, component
```

Хранятся executor, deadline, status, risk, estimated_hours, budget, metrics_json.

Smoke:

```text
[OK] Change links SQL/test perspectives and stores executor metrics
```

### 4.4 Transformation Graph

✅ Выполнено.

Схема содержит четыре отдельные сущности координации:

```text
transformation_sets
transformation_graphs
transformation_graph_nodes
transformation_alignments
```

Ограничение `UNIQUE(set_id, layer)` и CHECK по четырём значениям создают в одном Transformation Set четыре самостоятельных графа с разными ID:

```text
Knowledge | Implementation | Project | Resource
```

`transformation_graph_nodes` хранит независимый состав каждого графа. `transformation_alignments` согласует узлы разных графов; CHECK запрещает alignment графа с самим собой. API создания Transformation Set транзакционно создаёт 4/4 графа. Метрики отдельно агрегируют часы, бюджет и критический путь.

Smoke:

```text
[OK] Transformation metrics aggregate four-layer resource and financial data
[OK] Transformation Set contains four standalone coordinated graphs with cross-graph alignment
```

### 4.5 Review как объект первого класса

✅ Выполнено.

Полная область ревью:

```text
Project → Epic → Feature → Artifact → Version → Fragment
```

Физические таблицы: `epics`, `features`, `artifacts`, `artifact_versions`, `fragments`, `review_scopes`.
`POST /api/reviews` проверяет всю цепочку одним JOIN и отклоняет неполную либо межworkspace-цепочку. Сохраняются author, executor, status FSM, votes и immutable history. Сервис нормализации перевёл 4/4 seed-review scopes в полную шестиступенчатую модель.

Smoke:

```text
[OK] full Project→Epic→Feature→Artifact→Version→Fragment ReviewScope, voting, FSM and history
```

### 4.6 Execution Graph

✅ Выполнено.

Иерархия:

```text
Workspace → Portfolio → Program → Project → Graph
Project → Pipe → Release
```

Независимые M:N-связи:

```text
sprint_pipes
sprint_work_items
pipe_work_items
```

Smoke:

```text
[OK] Portfolio/Program/Project/Pipe/Release graph and independent Sprint-Pipe M:N links
```

### 4.7 Work Item → Issue

✅ Выполнено.

Issue: Problem, Risk, Constraint, KnowledgeDefect.
WorkItem связан через `issue_id`, имеет собственный FSM и ресурсные показатели.

Smoke:

```text
[OK] Issue to WorkItem hierarchy, metrics and persisted FSM transition
```

### 4.8 Pipe как конфигурируемая возможность

✅ Выполнено.

`pipes.stages_json` не фиксирует стадии жёстко. `issue_pipes` связывает Issue с процессом исполнения.

HTTP smoke проверяет создание Pipe со стадиями и связи Issue↔Pipe, Sprint↔Pipe, Pipe↔WorkItem.

### 4.9 Заказчик ≠ Owner

✅ Выполнено.

Роль хранится на Actor↔Object в `role_bindings`.
SQLite triggers `trg_role_customer_owner_insert` и `trg_role_customer_owner_update` запрещают одному Actor одновременно быть Заказчиком и Owner одного объекта.

Smoke:

```text
[OK] Customer and Owner separation is enforced; Interest Scope is computed from role bindings
```

### 4.10 Interest Scope

✅ Выполнено.

`GET /api/interest-scope/:actorId` вычисляет область интересов из WorkItem, role bindings, ReviewScope, Change, Issue и соседних связей графа.

Smoke проверяет попадание связанного узла и `roleBindingIds` в ответ.

### 4.11 Actor вместо Person

✅ Выполнено.

Допустимые типы на уровне БД и API:

```text
Human | AIAgent | Service | ExternalSystem
```

Smoke:

```text
[OK] Actor CRUD supports Human, AIAgent, Service and ExternalSystem model
```

### 4.12 Default First, Configure Second, Extend Third

✅ Выполнено.

`DEFAULT_PROFILE.principle`:

```text
Default First → Configure Second → Extend Third
```

NodeType и EdgeType материализуются в отдельные таблицы; `/api/ontology/extend` расширяет профиль выбранного графа.

### 4.13 Разбивка на движки

✅ Выполнено.

Backend-модули:

```text
backend/src/engines/graph.js
backend/src/engines/fsm.js
backend/src/engines/review.js
backend/src/engines/ontology.js
backend/src/engines/rag.js
```

Visualization:

```text
frontend/src/components/FlowCanvas.tsx
frontend/src/graphLayout.ts
frontend/src/graphFocus.ts
```

### 4.14 PI как слой над React Flow

✅ Выполнено.

React Flow изолирован в `FlowCanvas.tsx`. Ролевые проекции, фильтрация, подсветка, поиск пути, layout-защита и Delivery Control находятся выше библиотеки визуализации.

### 4.15 Graph Copilot

✅ Выполнено.

Объекты:

```text
Conversation
Question
Answer
ReasoningStep
Decision
```

Question хранит text/message, intent, actor, role, tab, diagram, cost, tokens, latency, feedback.
Graph Context Builder передаёт узлы, связи, Work Items и RAG chunks.

Smoke:

```text
[OK] Copilot persists Conversation, Question, Answer, Reasoning, Decision and usage feedback
```

Offline AI отделён от базового требования. `OFFLINE_AI_ENABLED=0` по умолчанию. `AI_EXECUTION_MODE` имеет явные режимы `hybrid | external | offline | local`. Причина расширения, границы данных и opt-out зафиксированы в `docs/adr/ADR-002-offline-ai.md` и возвращаются `GET /api/ai/capabilities`.

Smoke:

```text
[OK] Offline AI is explicitly optional, disabled by default and documented by ADR
```

### 4.16 Workspace-иерархия

✅ Выполнено.

Факты:

```text
workspaces.parent_id → workspaces.id
portfolios.workspace_id
programs.portfolio_id
projects.program_id / portfolio_id
graphs.project_id
```

Новый граф и импортированный JSON-граф автоматически получают Project в Default Program / Default Portfolio, если projectId не передан.

Повторное использование реализовано M:N-таблицами:

```text
workspace_resources
project_resource_links
project_node_links
```

Один Workspace Resource и один Node могут быть связаны с несколькими Project без копирования объекта. Smoke связал одну сущность с двумя разными проектами и прочитал её из обоих:

```text
[OK] one workspace resource and one graph node are reused by two projects through M:N links
```

### 4.17 RBAC / банковский сценарий

✅ Выполнено на уровне проверяемой модели доступа.

Таблицы:

```text
rbac_roles
rbac_permissions
rbac_role_permissions
rbac_assignments
object_acl
security_audit_log
```

Наследование:

```text
viewer → reviewer → editor → workspace_admin
```

Матрица содержит отдельные права `workspace.*`, `project.*`, `graph.*`, `review.*`, `rag.*`, `ai.use`, `rbac.manage`, `audit.read`. Assignment ограничивается workspace/project/graph/object. Явный `object_acl deny` имеет приоритет над наследуемым allow. Graph mutations, Review mutations, Copilot, Project/Template и Graph management вызывают проверку соответствующего permission. Изменения RBAC записываются в `security_audit_log`.

Smoke:

```text
[OK] inherited RBAC matrix, viewer read-only role, object-level deny priority and security audit log
```

### 4.18 Итог по 16 требованиям

```text
Функционально выполнено: 16 / 16
Согласованное исключение: PostgreSQL не реализован; используется SQLite WAL.
```

## 5. Сборка и тесты

### 5.1 Полная команда

```bash
sh scripts/audit.sh
```

### 5.2 Итоговый вывод

```text
[1/9] Reproducible dependencies
added 48 packages, and audited 49 packages in 1s
found 0 vulnerabilities
added 120 packages, and audited 121 packages in 14s
found 0 vulnerabilities

[2/9] Dependency security audit
found 0 vulnerabilities
found 0 vulnerabilities

[3/9] Frontend TypeScript
> graph-platform-frontend@3.2.0 check
> tsc --noEmit

> graph-platform-frontend@3.2.0 test:layout
> node --test test/graph-layout.test.mjs
✔ keeps an already safe layout unchanged
✔ separates duplicate and too-close coordinates
✔ recovers from invalid imported coordinates
tests 3
pass 3
fail 0

[4/9] Frontend production build
vite v8.1.5 building client environment for production...
✓ 192 modules transformed.
dist/index.html                   0.68 kB │ gzip:   0.39 kB
dist/assets/index-cxpchSCP.css   94.85 kB │ gzip:  18.75 kB
dist/assets/index-CcROsUC7.js   438.50 kB │ gzip: 134.89 kB
✓ built in 464ms

[5/9] Backend JavaScript syntax (all source files)
Checked backend files: 40

[6/9] SQLite schema and versioned migrations
✔ SQLite schema and all versioned migrations are applied
✔ Self-hosting Engine updates graph nodes when a tracked source changes
tests 2
pass 2
fail 0

[7/9] Python syntax
Checked Python files: 3

[8/9] Isolated backend with a temporary SQLite database
Backend ready on temporary port 31991

[9/9] API integration smoke
[OK] health v3
[OK] public catalog without auth (10 domains)
[OK] internal validation ecosystems and self-hosting graph
[OK] self-hosting repository sync (74 sources)
[OK] OpenAPI specification is served
[OK] built-in Dentist Choice graph (22 nodes, 26 relations)
[OK] seeded reviews have normalized ReviewScope and status
[OK] guest /graphs exposes public only
[OK] guest mutation blocked
[OK] signup
[OK] auth/me
[OK] personal workspace isolation
[OK] authenticated user still sees public catalog
[OK] create private domain in own workspace
[OK] read own graph
[OK] member JSON import policy
[OK] ordinary account imports private JSON graph with layout
[OK] JSON profanity moderation blocks package
[OK] JSON structural validation blocks missing relation target
[OK] guest node mutation blocked
[OK] create node with stored layout
[OK] edit node and persist drag layout
[OK] create second node
[OK] create relation between graph nodes
[OK] cross-graph relation blocked
[OK] edit and read relation
[OK] cross-workspace private graph isolation
[OK] imported member graph remains private to its workspace
[OK] FSM machine and transition
[OK] default ontology and graph-scoped extension
[OK] NodeType and EdgeType are materialized entities
[OK] Actor CRUD supports Human, AIAgent, Service and ExternalSystem model
[OK] Customer and Owner separation is enforced; Interest Scope is computed from role bindings
[OK] RAG ingest and graph-scoped retrieval
[OK] Issue to WorkItem hierarchy, metrics and persisted FSM transition
[OK] Change links SQL/test perspectives and stores executor metrics
[OK] Portfolio/Program/Project/Pipe/Release graph and independent Sprint-Pipe M:N links
[OK] Transformation metrics aggregate four-layer resource and financial data
[OK] Transformation Set contains four standalone coordinated graphs with cross-graph alignment
[OK] one workspace resource and one graph node are reused by two projects through M:N links
[OK] full Project→Epic→Feature→Artifact→Version→Fragment ReviewScope, voting, FSM and history
[OK] inherited RBAC matrix, viewer read-only role, object-level deny priority and security audit log
[OK] Offline AI is explicitly optional, disabled by default and documented by ADR
[OK] template snapshot preserves graph and layout
[OK] Copilot persists Conversation, Question, Answer, Reasoning, Decision and usage feedback
[OK] delete relation
[OK] delete nodes with relation cleanup
[OK] cleanup template
[OK] cleanup standalone program
[OK] cleanup imported member graph
[OK] cleanup own graph

Graph Platform v3 smoke test passed.
AUDIT RESULT: PASS
```

Количество smoke-проверок: 51.
Ошибок smoke: 0.

### 5.3 Предупреждение npm

При чистой установке npm выдал предупреждение:

```text
npm warn deprecated prebuild-install@7.1.3: No longer maintained.
```

Это предупреждение транзитивной зависимости. `npm audit` для backend и frontend вернул `found 0 vulnerabilities`.

## 6. Итог без оценочных формулировок

```text
Сущности ТЗ в применённой SQLite БД: 19 / 19
Функциональные требования ТЗ: 16 / 16
Runtime API-операции: 146
Операции в OpenAPI: 146 / 146
Версионированные миграции: 10 / 10 применены
Layout tests: 3 / 3 pass
Backend tests: 2 / 2 pass
API smoke: 51 / 51 pass
TypeScript: exit code 0
Production build: exit code 0
Backend syntax: 40 / 40 файлов прошли node --check
Python syntax: 3 / 3 файла прошли py_compile
Frontend npm audit: 0 vulnerabilities
Backend npm audit: 0 vulnerabilities
PostgreSQL: не реализован по согласованному исключению
Фактическая БД: SQLite WAL
```

## 7. Проверка пяти замечаний от 28.07.2026

```text
1. Transformation Graph: ✅ 4 самостоятельных графа + общие Node ID + alignment API.
2. Review Scope: ✅ Project → Epic → Feature → Artifact → Version → Fragment.
3. Workspace reuse: ✅ один Resource/Node связан с двумя Project через M:N.
4. RBAC: ✅ матрица прав + наследование + scoped assignments + object ACL + audit log.
5. Offline AI: ✅ оформлен как отключаемое расширение, disabled by default, ADR + runtime capabilities.

Закрыто фактическими HTTP-проверками: 5 / 5.
```

Конец отчёта.
